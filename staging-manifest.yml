kind: ExternalService
name: fandogh-staging123123
spec:
  env:
    - name: API
      value: https://api.mahmole.com/fa
  image: start:${IMAGE_VERSION}
  image_pull_policy: IfNotPresent
  port: 80
  replicas: 2
  resources:
    memory: 200Mi
