.checkbox-fan input[type="checkbox"] {
    opacity: 0;

}

.checkbox-fan label::before{
    content: "";
    display: inline-block;
    height: 20px;
    width: 20px;
    background #1b006a
    border-radius 8px
    
}

.checkbox-fan label::after
    content: '';
    display: block;
    width: 6px;
    height: 12px;
    border: solid #fff;
    border-width: 0 2px 2px 0;
    transform: rotate(45deg);



.checkbox-fan
  box-sizing border-box
  label 
    position relative
    margin 14px



.checkbox-fan label::before,
.checkbox-fan label::after {
    position: absolute;
}
/*Outer-box*/
.checkbox-fan label::before {
    
    right: -27px
}
/*Checkmark*/
.checkbox-fan label::after {
    right: -22px;
    top: 0px;
}
/*Hide the checkmark by default*/
.checkbox-fan input[type="checkbox"] + label::after {
    content: none;
}
/*Unhide the checkmark on the checked state*/
.checkbox-fan input[type="checkbox"]:checked + label::after {
    content: "";
}
.checkbox-fan input[type="checkbox"]:focus + label::before {
    outline: rgb(59, 153, 252) auto 5px;
}

.input-light
    label::before
        background #fff


    label::after
      border: solid #26c6ec;
      border-width: 0 3px 3px 0;
      transform: rotate(45deg);


.checkbox-fan.light
  label::before
    background #fff
  label::after
    border: solid #4fe6ff;
    content: '';
    display: block;
    width: 6px;
    height: 12px;
    border-width: 0 2px 2px 0;
    transform: rotate(45deg);

